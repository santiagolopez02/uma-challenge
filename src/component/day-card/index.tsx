import { DayCardProps } from "@/interfaces";
import React from "react";

const DayCard: React.FC<DayCardProps> = ({ image, title, date, coments }) => {
  return <div></div>;
};
export default DayCard;
